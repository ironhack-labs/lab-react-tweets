import React from 'react'

function Timestamp(props) {
    return (
        <div>
            <span className="timestamp">{props.timestampData}</span>
        </div>
    )
}

export default Timestamp